# Google Gemini API Key for AI-powered quiz generation
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your_api_key_here

# HTTP Basic Authentication credentials
# Set these to protect your API endpoints from unauthorized access
# If not set, the API will be unprotected (not recommended for production)
AUTH_USERNAME=your_username_here
AUTH_PASSWORD=your_password_here

# Server Port Configuration
# Port for the HTTP server (default: 3000)
PORT=3000

# Redis Configuration for API Cost Tracking
# Multiple environment variable patterns are supported for compatibility with various platforms
# Set ONE of the following (in priority order):

# Option 1: Full connection string (Vercel, Railway, most platforms)
REDIS_URL=
# REDIS_URI=
# REDIS_CONNECTION_STRING=

# Option 2: Individual components (some hosting platforms auto-generate these)
# REDIS_HOST=
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Note: When any Redis configuration is set, HTTP Basic Auth is automatically disabled

# API Cost Limit in USD
# Maximum allowed API cost before blocking requests (default: 10.0)
MAX_API_COST_USD=10.0
